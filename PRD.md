# KIS AutoRebalancer — PRD

- 문서 상태: v0.3 — 4개 FINDING 전부 수정 완료, 멀티 증권사 지원은 범위 제외로 결정
- 작성일: 2026-09-16 (최종 수정 2026-09-17)
- 근거: 운영 시트 스냅샷 + `kis_library_public/` 전체 정독 + 실제 시트에서 라이브 검증
- 온라인 버전(다이어그램 포함): https://claude.ai/artifact/W4hPDiViYpSPwH1yTPcnmn

## 1. 목적

한국투자증권(KIS) 계좌를 목표 비중에 맞춰 자동 리밸런싱하는 Google Apps Script 시스템.
사용자는 계좌 하나를 직접 운용하는 개인이며, 목표는 "안정적인 수익률을 유지하면서 필요할 때
그 돈의 일부를 실제로 꺼내 쓸 수 있는 상태"다. 리밸런싱 자체가 목적이 아니라, 자산을 불리는
동시에 유동성을 확보하는 것이 목적이다.

현재 시스템은 ①목표 비중 유지(리밸런싱), ②정기 소액 인출(월 인출 추천)까지는 구현돼 있다.
③계획에 없던 긴급 인출·긴급 매도가 매끄럽지 않다는 것이 이번 진단의 초점이었다.

**→ 4절의 FINDING 01~04는 전부 수정 완료됐다 (5절).** 남은 건 멀티 증권사 지원(6절, 이 프로젝트
범위에서는 제외 결정)과 7절의 열린 질문 몇 가지뿐이다.

## 2. 오늘자 계좌 스냅샷 (2026-09-16 15:29)

| 항목 | 값 |
|---|---|
| 총 평가액 | 9,335,815원 |
| 예수금(즉시 인출 가능) | 778,430원 |
| 평가손익 | −753,768원 (−8.10%) |
| 목표 수익률(연) | 10.0% |
| 월 인출 추천 | 74,445원 |
| 보유 종목 | 6종목 + 현금 |
| 리밸런싱 대상 | 3종목 |

## 3. 기능 인벤토리

| 기능 | 설명 | 파일 | 상태 |
|---|---|---|---|
| 대시보드 | 실시간 평가액·비중·리밸런싱 필요 종목 계산 | Dashboard.js | 정상 |
| 목표 포트폴리오 설정 | 종목 추가/검색/비중 조정, 기준비율(고정)·운용비율(조정값) 2단 구조 | PortfolioManager.js | 정상 |
| 리밸런싱 실행 | 목표 대비 초과분 매도 → 부족분 매수, 매도/매수 임계치 비대칭 | Dashboard.js, code.js | 정상 |
| 차선유지(정기 자동매매) | 지정 요일·시간 주간 트리거, 13일 쿨다운 | Menu.js, code.js | 정상 |
| AI 비중 제안 | Gemini가 조정안 제시, 사람이 승인 후 반영 | AIAdvisor.js | 정상 |
| FSD 드라이빙 모드 | 기술지표(TA) 기반 매도 강도 조절값(Chill~Mad Max) | AIAdvisor.js, TechnicalAnalysis.js | 부분 — 항상 Standard로 동작. 모드를 바꾸는 메뉴가 없음(`setFSDMode*`가 `27fb10a`에서 삭제됨, V2 코드베이스에만 남아있음) |
| 비중 락(Layer 1) | 비중 변경 후 재변경 제한 | Menu.js | 정상 |
| 수익 실현(현금화) | ①비중 유지 비례매도 ②종목 선택 매도, 2가지 모드 | Withdraw.js | 정상 |
| 보호 예수금(2주) | 인출액을 리밸런싱 매수 여력에서 실제로 제외 | Withdraw.js, Dashboard.js | 정상 |
| 종목 개별 강제 매도 | 목표 비중과 무관하게 종목 하나를 즉시 시장가 전량매도 | EmergencySell.js | 정상 |
| 긴급 정지 | 차선유지(정기 자동매매) 트리거를 무조건 끄는 원웨이 동작 | Menu.js (`emergencyStopAutomation`) | 정상 |
| 거래내역/수익실현/비중변경/기술지표 로깅 | 모든 매매·AI 판단·지표 변화를 시트에 기록 | SheetManager.js, Dashboard.js | 정상 |
| API 키 보안 저장 | KIS/Gemini 키를 UserProperties에 시트별 격리 저장 | SecureConfig.js, config.js | 정상 |

## 4. 문제 진단 — 긴급 대응이 안 되는 4가지 이유

사용자가 묘사한 두 시나리오 — ①전체적으로 100만 원 정도 현금화, ②특정 보유 종목을 다 팔기 —
를 기준으로 코드를 추적했다. ①은 있지만 안전하지 않고, ②는 아예 없다.

**FINDING 01 — 보호 예수금이 실제 리밸런싱 계산에서 무시된다. (✅ 수정 완료 — 5절)**
수익 실현을 실행하면 `setProtectedCash()`가 인출액과 14일 만료 시각을 `PropertiesService`에
저장한다(`Withdraw.js:259-265`). 하지만 매수 가능 금액을 계산하는 `calculateRebalancePlan()`은
이 값을 한 번도 읽지 않고 `balance.buyPower`를 그대로 쓴다(`Dashboard.js:471`). 현금화 직후
차선유지가 돌면 인출하려던 돈을 "여유 현금"으로 인식해 즉시 재매수할 수 있다. 시스템 상태
화면의 "보호 예수금 ○일 남음" 배지는 장식일 뿐 실제 매수 차단으로 이어지지 않는다.

**FINDING 02 — 종목 하나를 지금 당장 전량 정리하는 기능이 없다. (✅ 수정 완료 — 5절)**
포트폴리오 관리 화면에서 종목을 삭제하거나 비중을 0%로 바꿔도, `getTargetPortfolio()`
(`code.js:239`)는 그 종목을 목표 계산에서 빼기만 한다. 보유 수량을 매도하는 코드는 연결돼
있지 않다 — 오히려 최근 커밋에서 "목표외 종목 청산 로직"을 되돌렸다
(`7058dcb Revert: 목표외 종목 청산 로직 제거`).

**FINDING 03 — "현금화"가 비례 매도 한 가지 방식으로만 동작한다. (✅ 수정 완료 — 5절)**
`openWithdrawDialog()`의 계산기는 목표 금액을 입력하면 모든 보유 종목의 비중을 유지한 채
초과분만 얇게 잘라 판다. 종목을 골라서 매도하는 옵션이 없다 — 100만 원이 필요할 때 6개
종목에 걸쳐 소액 매도가 분산 발생해 슬리피지와 매도세(0.215%) 부담이 커진다.

**FINDING 04 — 긴급 상황에서 자동매매를 잠깐 멈추는 스위치가 없다. (✅ 수정 완료 — 5절)**
코드베이스 전체에서 `PAUSE`, `EMERGENCY`, "일시정지" 키워드가 0건이다. "차선유지"가 켜져
있으면 사용자가 무엇을 하고 있든 예정된 요일·시간에 자동 리밸런싱이 그대로 실행된다.
FINDING 01의 재매수 위험을 사용자가 수동으로 피할 방법이 차선유지 전체를 끄는 것뿐이다.

**근본 원인.** 이 시스템은 "목표 비중을 정하고 다음 정기 리밸런싱 사이클에 반영한다"는 단일
경로로만 설계돼 있다. 계획적인 변경(비중 조정, 월 정기 인출)은 이 경로로 잘 동작하지만,
지금 이 순간 즉시 개입해야 하는 경로 — 즉시 전량매도, 즉시 정지, 인출액의 실질적 격리 —
는 설계 자체가 없거나(FINDING 02, 04) 만들다가 배선이 끊긴 상태(FINDING 01)다.

## 5. 개선 우선순위

| 우선순위 | 내용 | 상태 |
|---|---|---|
| P0 | 보호 예수금을 실제 계산에 반영 | ✅ 완료 — `Dashboard.js`의 `effectiveBuyPower`가 `getProtectedCash().amount`를 차감하도록 수정 |
| P1 | 보유 종목 "즉시 전량매도" 버튼 | ✅ 완료 — 메뉴 → 🚨 긴급 대응 → 🔴 종목 즉시 전량매도 (`features/EmergencySell.js`) |
| P1 | 메뉴에 "🚨 긴급 정지" 토글 | ✅ 완료 — 메뉴 → 🚨 긴급 대응 → 🚨 자동매매 긴급 정지 (`emergencyStopAutomation()`, `Menu.js`) |
| P2 | 수익 실현 다이얼로그에 "종목 선택 매도" 모드 | ✅ 완료 — 🎯 종목 선택 매도 모드 추가 (`Withdraw.js`), 비중 유지 비례매도 모드는 그대로 유지 |

> **"보호 예수금이 있으면 자동매매를 건너뛰고 알림"은 별도 항목에서 뺐다.** `effectiveBuyPower`가 보호 금액을 이미 매수 여력에서 제외하므로, 스케줄된 리밸런싱이 돌아도 그 돈으로는 매수 주문 자체가 나오지 않는다 — 전체를 건너뛰게 만들면 나머지 95% 자산의 정상적인 리밸런싱까지 최대 14일 막히는 부작용이 더 크다.

### 5.1 UI 접근성 개선 (2026-09-17)

FINDING들을 고치는 것과 별개로, "쉽게 쓸 수 있게"라는 요청에 맞춰 두 가지를 바로 손봤다.

- **메뉴 순서 변경** — `🚨 긴급 대응`을 메뉴 맨 위(새로고침·리밸런싱 실행 바로 다음)로 올렸다.
  원래는 AI 분석·포트폴리오 관리·수익 실현을 다 지나야 나오는 4번째 그룹이었다 — 급할 때 쓰는
  기능이 가장 늦게 나오는 게 맞지 않았다.
- **📖 기본 사용법 안내 갱신** — 기존 가이드는 이미 코드에서 사라진 "AI 자율 포트폴리오 관리"와
  FSD 5단계 모드(Chill~Mad Max) 설명을 그대로 담고 있었다 — 실제로는 메뉴에서 그 모드를 바꿀
  방법이 없는데도(관련 함수가 `27fb10a` 커밋에서 삭제됨) 가이드만 안 지워진 상태였다. 그 내용을
  지우고, 오늘 추가된 기본 설정·포트폴리오 비중 자동계산·긴급 대응·종목 선택 매도를 반영했다.

## 6. 멀티 증권사 지원 — 검토했으나 이 프로젝트 범위에서 제외

**결정 (2026-09-17): 이 Google Sheets/Apps Script 프로젝트는 한국투자증권(KIS) 전용으로 유지한다.**
나무증권·토스증권은 별도로 작업한다 — 이 시트/코드베이스에는 포함하지 않는다.

이 결정에 따라, 세션 중 만들었던 `core/KISClient.js` → `core/BrokerClient.js`(디스패처) +
`brokers/KISAdapter.js`(KIS 구현) 분리를 되돌렸다. 이 프로젝트 안에서는 KIS 외 증권사로 분기할
일이 없으므로, 디스패처가 있으면 "나중에 나무/토스를 붙일 수 있다"는 신호만 주고 실제로는 죽은
코드가 된다. `getBalance/getHoldings/getCurrentPrice/placeOrder`는 다시 `core/KISClient.js`
하나에서 KIS API를 직접 호출한다.

아래 6.1 API 비교는 나무·토스를 별도 프로젝트로 만들 때 참고할 수 있어 남겨둔다.

### 6.1 API 비교 (참고용 — 별도 프로젝트에서 활용)

| 항목 | KIS(현재) | 나무증권 · NH PLUG | 토스증권 |
|---|---|---|---|
| 인증 | appkey/appsecret → Bearer 토큰 | appkey/appsecret → Bearer 토큰 | OAuth2 client_credentials → Bearer 토큰 |
| 토큰 발급 | `POST /oauth2/tokenP` | `api.nhplug.com:8443` (24h 캐시) | `POST /oauth2/token` (form-urlencoded) |
| 잔고 조회 | tr_id `TTTC8434R` | `/krstock/inquiry/v1/balance` | `GET /api/v1/assets` |
| 매수/매도 | 단일 `order-cash` + `ORD_DVSN` | 엔드포인트 분리: `cashBuy` / `cashSell` | 단일 `POST /orders` (필드 미공개) |
| 모의투자 | 완전 분리된 서버 | 부분 지원 — 토큰은 실전 서버에서만 발급 | 없음 — 운영 서버뿐 |
| 필드 네이밍 | `CANO`, `ACNT_PRDT_CD` | `act_no`, `bnc_bse_cd` | `accountSeq`, `symbol` |

**리스크.**
- 토스는 모의투자 환경이 없다 — 어댑터 검증에 소액 실거래가 필요하다.
- 토스 주문 API(`POST /api/v1/orders`)는 공개 스펙에 요청 필드가 없다 — 개발자 계정으로
  직접 확인이 필요하다.
- 나무는 모의투자용 토큰도 실전 서버에서만 발급된다 — KIS처럼 완전히 분리된 mock 엔드포인트가
  아니다.

### 6.2 나무·토스는 어떻게 할 것인가

이 시트/코드베이스 밖에서 별도 프로젝트로 진행한다. 이 문서의 6.1 비교표와, 이번 세션에서
잠깐 만들었던 broker-interface 패턴(4개 메서드로 추상화)은 그 프로젝트를 시작할 때 참고할 수
있다. 계좌 운용 모델(증권사별로 독립 운용할지, 통합할지)은 그 별도 프로젝트를 시작할 때 다시
정한다.

## 7. 열린 질문 → 구현하면서 정해진 것

구현 전에는 열린 질문이었지만, 실제로 만들면서 아래로 정해졌다.

- **즉시 전량매도의 확인 UX** → 원클릭이 아니라 `confirm()` 확인창을 한 번 거친다
  (`EmergencySell.js`의 `sellAll()`). 급한 상황이라도 종목명·수량을 다시 보여주고 확인받는 쪽을
  택했다 — 실수로 잘못 누른 종목이 전량매도되는 게 더 큰 리스크라고 판단.
- **보호 예수금 배선 수정의 적용 시점** → 전체를 잠그지 않는다. `effectiveBuyPower`에서 보호
  금액만 빼고, 나머지 자산은 평소처럼 정상 리밸런싱된다 (5절 하단 설명).
- **긴급 정지의 범위** → 지금은 "차선유지"만 멈추면 충분하다. 이 코드베이스(Public)에는 AI가
  스스로 매매를 실행하는 자동 트리거가 없다 — 그 기능(FSD 자율매매)은 `27fb10a` 커밋에서 이미
  빠졌고, 별도로 존재하는 V2 코드베이스에만 남아있다. V2를 이 프로젝트에 다시 합치게 되면 이
  질문을 재검토해야 한다.
